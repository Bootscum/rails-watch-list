<h1 class="text-light">Here is your list!</h1>
<%# <%= @list.name %>
<%# <%= @list.movies.each do |movie| %>
<%# <h3><%= movie.title %></h3>
<%# <h6><%= movie.overview %></h6>
<%# <h6><%= movie.rating %></h6>
<%# <% end %>
<%# <%= link_to "Back to the main page", lists_path %>
<%# <h3>Edit your list?</h3>
<%= link_to "Edit List",  edit_list_path(@list)%>
<%# <%= link_to "Delete List", @list, data:{turbo_method: :delete, turbo_confirm: "Are you sure?"} %> %>


<div class="card" style="width: 18rem;">
  <img src="..." class="card-img-top" alt="...">
  <div class="card-body">
    <h5 class="card-title text-justify-center"><%= @list.name %></h5>
  </div>
  <ul class="list-group list-group-flush">
    <% @list.movies.each do |movie| %>
    <li class="list-group-item"><%= movie.title %></li>
    <% end %>
  </ul>
  <div class="card-body">
    <%= link_to "Edit List",  edit_list_path(@list), class:"btn btn-warning" %>
    <%= link_to "Delete List", @list, data:{turbo_method: :delete, turbo_confirm: "Are you sure?"}, class:"btn btn-warning"  %>
  </div>
</div>
